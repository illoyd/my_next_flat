.panel.panel-info
  .panel-heading
    .pull-right
      = link_to :back do
        %i.fa.fa-arrow-circle-o-left
        Back
    %h2.panel-title= @listing.address
    
  -#
    .col-md-6{ style: 'padding-left: 5px; height: 100%; min-height: 100px;' }
      -# #map_canvas{ style: 'height: 100%; min-height: 255px;' }
      = map_tag(@listing.address, height: 255, width: '100%')
      

  .panel-body
    .tiltshift{ style: "background-image: url('#{ @listing.image_url }')"}
      = image_tag @listing.image_url, src: @listing.caption

    .backgroundFrame
      .background{ style: "background-image: url('#{ @listing.image_url }')"}

    = link_to @listing.listing_url do
      = image_tag @listing.image_url, src: @listing.caption, class: 'thumbnail'
    %p
      .label.label-primary= price_for(@listing)
      .label.label-info= beds_for(@listing)
      .label.label-info= baths_for(@listing)
      ~ @listing.description
    %p= link_to 'More Information', @listing.listing_url, class: 'btn btn-success btn-block'

    -#
      %p
        %strong Raw
      %pre~ @listing.as_json

  .panel-footer
    Last updated #{ l(@listing.updated_at) }
    .pull-right
      = link_to :back do
        %i.fa.fa-arrow-circle-o-left
        Back
