-# Area...
%p
  I want to search near
  %code a point of interest
  \...

%table.criteria
  %tbody
    %tr
      %td= f.label :radius, iconify('Radius', 'map-marker')
      %td= f.select :radius, options_for_radius, {}, class: 'form-control'
    %tr
      %td= f.label :point_of_interest_id, iconify('POI', :search)
      %td= f.collection_select :point_of_interest_id, PointOfInterest.points_of_interest, :id, :name, {}, class: 'form-control'
